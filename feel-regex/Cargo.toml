[package]
name = "dsntk-feel-regex"
version = "0.0.0"
authors = { workspace = true }
description = "DSNTK | FEEL regular expressions"
documentation = "https://docs.rs/dsntk-feel-regex"
repository = "https://github.com/dsntk/dsntk-rs.git"
license = { workspace = true }
edition = { workspace = true }

[features]
default = []
oniguruma = ["onig"]

[dependencies]
onig = { version = "6.4.0", default-features = false, optional = true }
